# Delivery Agent Notes

This branch is moving Rcode toward a delivery-first coding agent.

Primary focus:

- implement features
- fix bugs
- run tests
- review diffs
- prepare PRs

The runtime changes are concentrated in `server/` and the skill library changes are in `src/skills/`.
