# {{DISPLAY_NAME}}

Generated Kun Node host + sandboxed React Webview extension for Extension API
1.x. React runs only in the extension Webview, never inside Kun's React tree.

```sh
npm install
npm test
npm run validate
npm run pack
```

Developer documentation: https://kun.dev/extensions/1/
